# CircleCI Configuration - Fresh Start
# Placeholder configuration to start with a clean slate

version: 2.1

jobs:
  placeholder-job:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "This is a placeholder job."

workflows:
  placeholder-workflow:
    jobs:
      - placeholder-job
